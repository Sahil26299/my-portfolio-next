import React from "react";

const Loading = () => {
  return (
    <main className="h-screen w-screen flex flex-col items-center justify-center text-lg-1 text-white">
      Loading...
    </main>
  );
};

export default Loading;
